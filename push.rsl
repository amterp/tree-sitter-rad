#!/usr/bin/env rad
---
Runs things and pushes.
---
args:
    skip_tests "skip-tests" T bool
    skip_push "skip-push" P bool

if not skip_tests:
    $!`./dev -u`

$!`git add .`
$!`git commit`

if not skip_push:
    $!`git push`
